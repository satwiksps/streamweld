# Required on Vercel: proxy demo API calls to a shared Worker/D1 backend.
# The in-memory fallback is deliberately limited to local emulation because
# independent serverless instances cannot provide durable resume semantics.
STREAMWELD_DEMO_UPSTREAM_ORIGIN=https://demo-api.example.com
