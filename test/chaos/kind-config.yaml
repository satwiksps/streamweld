kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
    labels:
      streamweld-chaos-role: backend
  - role: worker
    labels:
      streamweld-chaos-role: backend
  - role: worker
    labels:
      streamweld-chaos-role: system
networking:
  ipFamily: ipv4
